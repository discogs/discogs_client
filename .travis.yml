language: python
python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"
    - "3.4"
install:
    - pip install -r requirements.txt --use-mirrors
script:
    - nosetests --with-coverage --cover-package=discogs_client
after_success:
    - coveralls
